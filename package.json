{
  "name": "eadp-cli",
  "version": "0.13.1",
  "description": "面向 EADP 多环境的资源与权限编排命令行工具",
  "type": "module",
  "bin": {
    "eadp": "dist/bin.js"
  },
  "files": [
    "dist",
    "skills",
    "README.md"
  ],
  "scripts": {
    "build": "node ./scripts/clean-dist.mjs && tsc -p tsconfig.json",
    "pretest": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:live:feature-pagination": "node ./scripts/live-feature-pagination-test.mjs",
    "presmoke": "npm run build",
    "smoke": "node ./scripts/smoke-test.mjs",
    "test:package": "node ./scripts/package-install-test.mjs",
    "install:local": "npm pack && node ./scripts/install-local-package.mjs && node ./scripts/install-local-skill.mjs",
    "release": "node ./scripts/release.mjs",
    "check": "npm run build && npm test",
    "prepack": "npm run check",
    "dbg": "node debug-pack.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "keywords": [
    "eadp",
    "cli",
    "api",
    "workflow"
  ],
  "license": "UNLICENSED",
  "dependencies": {
    "commander": "^14.0.0",
    "yaml": "^2.8.1",
    "zod": "^4.1.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^6.0.0",
    "vitest": "^3.2.4"
  }
}
